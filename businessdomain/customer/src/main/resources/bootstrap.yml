## YAML Template.
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      failFast: true
      #name of base properties config file, before profile
      name: config-client
      profile: development
      #label: master
      username: admin
      password: 123456
      retry: 
        max-attempts: 1
        max-interval: 1000